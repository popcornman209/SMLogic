more lua functions
	select, lets the user select stuff based off things.

you can type in lua editor and use keybinds at same time

keybind customizer

tooltips on buttons and settings

tab key for renaming or tick step doesnt get blocked

windows has no icon on the file itself

export circuit as png

more fast connect tool modes

dragable connections to sperate them out

"dead end" detection, ability to remove gates that dont contribute to the output in any way
